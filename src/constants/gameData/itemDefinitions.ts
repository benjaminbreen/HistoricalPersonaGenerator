/**
 * Placeholder for item definitions - not needed for persona generator
 */

export function getItemDefinition(itemId: string): any | null {
  // Return null for any item lookups - the persona generator doesn't need actual items
  return null;
}

/**
 * Placeholder for regional materials - not needed for persona generator
 */

export function applyRegionalMaterial(
  itemName: string,
  region: string
): string {
  // Return item name unchanged
  return itemName;
}

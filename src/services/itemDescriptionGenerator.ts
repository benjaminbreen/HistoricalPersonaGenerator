/**
 * Placeholder for item description generator - not needed for persona generator
 */

export function generateProceduralItemDescription(
  itemName: string,
  culturalZone: string,
  era: string
): string {
  return `A ${itemName}`;
}
